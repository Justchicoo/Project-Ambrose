<!-- Project Ambrose by Imjustchico: Ordered implementation roadmap: phases, milestones, and open decisions. -->

# Roadmap

Work through the phases in order. Each phase ends with something visible in the real client, and each milestone is small enough to implement and verify in one focused stretch. A milestone is done when every acceptance check in its phase file passes.

**Evidence names nothing personal.** Settled on 2026-09-22 at the maintainer's direction. A ticked acceptance check quotes what proved it, and this repository is public, so the quote uses a neutral stand-in wherever a real account, address, token, path or machine name would otherwise appear: `<account>` for a game or panel account, `<address>` for an address that is not loopback, and so on. What matters in the evidence is the behaviour the line shows, never whose account showed it. The same goes for sample data in the panel and for anything pasted into a doc. The forbidden file scan keeps out keys, stores, logs and token files, which is where such things usually leak from; this rule covers the ones a person types by hand.

**Every subsystem ships with its panel surface.** Settled on 2026-09-18 at the maintainer's direction, once the panel foundation is built: a milestone that adds a subsystem also adds what the panel needs to show and drive it, in the same milestone and with its own acceptance check. That means the fields it publishes into the status API, the admin endpoints and events it serves, and its page or section in the dashboard, in the look doc/DESIGN.md sets. Phase 17 then holds only the panel's own machinery, not a queue of pages waiting to be fitted to finished systems. A milestone whose subsystem has nothing to show says so in its deliverables.

## Where we are

1.12 closed on 2026-09-24, the crypto basics finished by an outside contributor: the stored checksum in the pinned install's archives matches the client's own CRC variant and not the standard one, and every GameData archive parses and reads its stored entries. Phase 1 is built through 1.20, and 1.06, 1.07 and 1.08 were closed on 2026-09-23 by an outside contributor who found their last check was stale: the locale round-trip it asks for has a client-gated test, and it passes on the pinned install. The phase covers toolchain and CI, utilities, configuration, logging, threading, crypto, the KIWAD reader, the runtime message registry, frames and control messages, the socket layer, and the gameserver, loginserver and patchserver app skeletons. 1.21 (the patch-free launcher) and 1.22 (the session handshake, keepalives and the loginserver bootstrap) are implemented and pass their automated checks. Their real-client checks need the maintainer's own client and follow doc/PATCHING.md. doc/PATCHING.md was brought level with what is built on 2026-09-24 by an outside contributor: `-P 0` is required of every development run rather than assumed, the launcher passes it on every run and refuses a configuration that asks for patching, which `LauncherTest.RefusesPatching` holds, and what the retail client does when `-P` is left off is recorded as unestablished rather than described, because nobody has run it. The milestone's own deliverable line, which still named the run-client scripts 3.25 replaced and had been garbled by that edit, now names src/tools/launcher. Until they are recorded, work continues on milestones that do not depend on them, starting with the database layer in phase 2. Phase 2 is built: 2.01-2.15 pass their automated checks. 2.09's real-client check was earned on 2026-09-25 by the client driver, whose enter-world run records the login server decoding MSG_USER_AUTHEN_V3 field by field, 2.10's was earned the same day by its announce-and-kick run, where a game master's announcement shows as the client's notification and a kick opens the client's own dialog for a disconnect by an administrator, and the real-client checks of 2.14 and 2.15 wait for the maintainer. In phase 4, 4.08's extractor reads every zone the pinned install holds, 3356 of them with no failures in 33 seconds, and writes zone templates, named locations and object placements with real positions, by an outside contributor on 2026-09-23. It stays open where it drops what it cannot decode without saying so, which costs six of WC_Hub's 183 objects and four of WC_Ravenwood's 97, and because spawn data is not extracted yet. 16.03 is in flight on 2026-09-24 from the same contributor and is the first thing the project has that turns a real installation into a manifest of its own: patchlist_generator walks the user's install, packages it by the client's own rules, caches each file's checksums by path, size and time, and writes LatestFileList.xml and .bin into a revision directory named from Bin/revision.dat. Run against the pinned r806919 install it reproduces Size, CRC, HeaderSize and HeaderCRC for 3589 of 3589 type 3 and type 5 records, which is the client's own CRC variant and its archive header length proved against 19 GB of real files, in 194 seconds, and a second run costs 1 second on 3827 cache hits and writes a byte-identical .bin. It stays open because package membership is 3820 of 3825 and four fields outside the checks are still wrong, which the phase file records. In phase 16, 16.01 is done, the first milestone finished whole by an outside contributor: the patch server reads and writes the client's binary table list, and a reference list of 1144891 bytes parses and re-emits byte for byte. 16.02 is done on 2026-09-24 from the same contributor: one in-memory manifest is read and written as both the client's XML and that binary form, keeping the table order the file itself declares rather than assuming About sits at either end of it. The pinned install's own LatestFileList.xml parses to 3591 tables, About beside 3590 file tables with 140 records in Base and 97 in PatchClient and one record naming Data/GameData/<table>.wad in every other, writes back with the same tables in the same order and the same records, and survives the binary form with a byte-identical re-encode. It was first built to the wrong XML shape, and that was this roadmap's fault rather than the contributor's: the deliverable line sketched a table as a fixed element with its name in an attribute, where the client gives every table its own tag. The line now says so. In phase 5, 5.07's cache is built and wired into the login server by an outside contributor on 2026-09-23: typeregbuild writes a revision-stamped binary registry the server builds from directly, 4.8 MB against the dump's 17.5 MB, loading in a third of the JSON path's time on the pinned install, and falling back to the dump when the cache is missing, stale or damaged. It stays open for its two remaining checks, a client-gated comparison of every class, property and enum table, and a measurement under 200 ms. 5.04's extractor is built on 2026-09-25: `extractor levels` and the game server's first start fill nine world tables from the user's own MagicXPConfig, school templates and stat configuration, 1267 level rows for the seven wizard schools up to level 180 with each school's own hitpoints over the shared table's other values, and sPlayerLevelMgr loads and reloads them. Its last check, that a reload reaches the next login, closes with 5.05, which is where a login first reads them. In phase 4, 4.04's own work is built by an outside contributor on 2026-09-23: coordinate and yaw packing, the location string parsed and formatted independently of the process locale, and the service 5 ordinals checked against the client's own XML. Its three remaining checks describe 4.05's work, so it stays open until that lands. 4.03 is done as of 2026-09-23: sRealmList holds the realms a player may be sent to and the rule that picks one, the realm the client named, else the operator's default, else the least full, and a realm whose heartbeat is older than the policy allows is gone from the list rather than waiting to be marked down, so a gameserver that stopped falls out by itself. A named realm that is down is refused rather than quietly swapped, because a player who asked for one world and was put in another would find none of what they left. A gameserver beats on the world tick, since a server whose tick has stopped is not one to send anybody to, and adds its own realm row the first time it runs so a first install has somewhere to send a player without anybody writing SQL by hand, never touching that row again because its address, port and player limit are the operator's and only the population, the beat and whether it is up are the server's. 4.05 is in flight as of 2026-09-23: picking a wizard is answered rather than ignored, so the client is told which gameserver to go to, at which zone and place, with a single-use key written to login_key and committed before the reply is sent, because the client carries that key straight on and a key the login server has not finished writing down would turn a legitimate player away. A wizard that is not the account's, one that is deleted and a pick with no realm online are each refused with Error=1 and leave no key behind. Its unit check is earned; the two that remain need a real client driven to character select, one to see the MSG_ATTACH it sends with that key and one to see Play reach a loading screen, so 4.05 and with it 4.04 stay open. 4.06 is in flight the same day and is what gives the game server a door at all: it reads the client's own message definitions, declares its own message table and listens on WorldServerPort, so a client that has been told where to go can arrive, handshake into a session that server offers and send MSG_ATTACH, which is taken while the session is only Connected because a client that has not attached has nothing else it may say. The whole handoff is proved end to end in one test, sign-in through pick, close, reconnect and attach, with the key the login server issued arriving at the game session unchanged. The login server now lets a client that has been sent onward leave of its own accord and closes it from this side only after Network.HandoffGrace, and PublicAddress names the address players off this machine reach a realm at when Realm.Address does not. The panel's Client data page is live on 2026-09-24 and was the one that looked finished and was not: it used no sample data, so nothing marked it as a mockup, but every value on it was typed in by hand. It now reads from the app itself, the install and whether its revision is the pinned one, the type dump with the revision, extractor and executable hash that made it and whether the dump matches the install, and how many message definitions were read from the client, and it says so plainly when there is no install, no dump, a dump that cannot be read or a dump of another revision. It carries facts about the install and never a client file's bytes, which is 17.20's own constraint. Rebuilding the data and switching an app to another install are 17.20's and wait on 3.23, so that milestone stays open. The panel's Realms and zones and Activity pages are live on 2026-09-24 beside it, on the same rule and with the same limits stated: realms are read from the login server's own list and judged online by the rule that decides where a client is sent, so the page and the player cannot disagree, and activity is each app's own command record read newest first, with a refused attempt shown like any other because what somebody tried and was not allowed to do is the half of a record that matters. Editing a realm's limits and flags is 17.31's and waits on 17.48 and 4.09; the activity page serves 17.25's data while its filters, retention and export wait for that milestone. The panel's Players online page is live on 2026-09-24, built with the milestone that gave it something to show rather than after it: 4.07 writes a row when a gameserver spends a handoff key, that row now carries the moment it was written, and the login server answers GET /api/players with every wizard in a realm, the account behind it, the realm and zone it is in and how long it has been on. A row whose wizard is no longer in the characters database is shown by its id rather than left out, because an operator needs to see a row that should not be there. The actions a game master will want, kick, mute, teleport and the account and ban pages beside them, are 17.21's and wait on 17.48, 3.17 and 6.05, so that milestone stays open and only its data is served. 4.15 was finished on 2026-09-25, when its last two checks found that no app had a messages reload target and that a journal export landed under the server's working directory rather than in the pending folder the updater reads, and it is the shape every later store reloads by: a store is replaced rather than edited, so a reader holding a snapshot keeps the whole generation it started with while the next one is built beside it, and a build that fails leaves what was serving in place and reports every error it found rather than the first. 4.16 followed the same day: every tunable value the servers read live is a typed setting declared once, listed in doc/config/settings.md, changed with `settings set` on a console or `.settings set` in game, persisted in the database the app owns with one audit row per change, and locked where an environment variable or override sets it; the persisted values are ConfigMgr's live layer, so every existing reader sees them, and a change to World.UpdateInterval moves the measured tick within two ticks. Configuration and the client's message definitions are the first two targets, a changed setting tells logging to apply it at once, and a reload is asked for from the panel's own page, from `.reload`, from the console, or by a hangup on Linux, each saying the same thing because one place decides the words. Four of its checks are earned, one of them the whole chain an operator runs when they edit a .conf file and reload: the file changes, the config target rebuilds from it, the subscriber logging registered is told which key changed, and routing follows with nothing stopped, while a file that cannot be parsed leaves the routing that was serving exactly as it was. Another was earned on 2026-09-24 under a thread sanitizer build on Linux, where sixteen of its tests run clean and a deliberate race added to the same file is caught, so the clean result means the sanitizer was watching rather than absent. The rest are open for reasons worth stating: the two integration checks for live log routing and for broken message XML are not written, and the world edit journal is built and tested but nothing feeds it, because no part of the server edits the world database yet, so the check that asks for a live edit cannot pass until one exists. 4.07 is built on 2026-09-24 and is what the key was always for: the game server spends it before letting anybody in, in one conditional update that names the key, the account, the wizard and the realm together, so two clients holding the same key cannot both win it and a replay changes nothing. A spend that changed no row is read back once, only to name why the client was turned away, whether the key was spent, expired, issued for another account, another wizard or another realm, or never issued at all, and the client is told with MSG_ATTACHFAILED and the socket closed behind it. A client that is let in has its wizard and account marked online for that realm and gives them back when it goes. This is also where the database layer learned to run a statement off the calling thread and still say how many rows it changed, which is what makes a conditional write provable rather than hopeful, and every later one will need it. Two tests that had held the old behaviour now hold the new: an attach is no longer believed because the client said so. 4.06's real-client check and its bad-key refusal, and 4.07's own real-client check, wait for a client at the keyboard, and 4.07's waits also on 4.14, since it asks to see LOGINCOMPLETE run after the key is accepted. In phase 3, 3.01, 3.03 and 3.04 are done, 3.02 passes its automated checks with its real-client check waiting for MSG_BADGES, 3.05 passes its automated checks with its real-client check waiting for the character list and creation, 3.06 is done, 3.07 is done, 3.08 is done, 3.09 passes its automated checks with its real-client checks waiting for the maintainer, 3.10 and 3.11 are done, 3.12 passes its unit check with its CharacterCreationConfig.xml checks waiting for 6.10 because the r806919 type dump does not list that file's classes, 3.13 and 3.14 are done, 3.20, added at the maintainer's direction, passes its automated checks with its terminal extraction offer still to be recorded. 3.21, added on 2026-09-17, is done: typeextract builds the type dump from the user's own client program. 3.28, added on 2026-09-23 at the maintainer's direction, is what makes that true of any client rather than of the two this project has tried: the server is already compiled against no revision, which Decisions, Data now states, and the version-specific knowledge that remains is the engine field offsets typeextract needs, which it will derive from the running client rather than hold as a table. 3.18 landed on 2026-09-23 from an outside contributor: the updater copes with a changed hash under Updates.Redundancy, an empty hash under Updates.AllowRehash, missing files under Updates.CleanDeadRefMaxCount and a pending folder gated behind Updates.AllowPending whose files are named rev_<unix-timestamp>_<slug>.sql, with all four acceptance checks earned; it stays in flight because the decision-logic unit tests it asks for, the ones that need no database, and its ARCHIVED and module-include deliverables are still to come. 8.14 landed the same day from the same contributor: a versionable object decoded from the client re-encodes byte for byte, proved on the hat template, TemplateManifest.xml compared after inflation and a 2000-file sample, with the test shown to fail when the write order is reversed; an audit on 2026-09-25 found both of its detailed-list checks earned by the same tests as the short list's, and a run against the pinned install passed all three, so 8.14 is done; the property-order preservation it adds is still inert on every file tested, which the phase's review notes now carry. The same audit found 3.15's creation check earned by the very run that earned 3.16's, since the two share it word for word. 6.09 landed the same day as a sweep rather than a probe: schemaprobe reads the whole of Root.wad in 32.5 seconds and lists the four classes the dump does not describe with exactly the instance counts the milestone predicts, but the oracle that was to name their properties produces nothing on a real run, so that half and the draft schema it feeds are still to come. 3.22 is done: a first start finds the install, builds its type dump and extracts the name tables without asking. 5.08 is in flight on 2026-09-24 from an outside contributor: apps/installer gives a checkout one command per step on either platform, `deps`, `compile`, `conf`, `db` and `run`, with conf/dist/env.dist carrying the prefix, build type and preset, and doc/INSTALL.md walking it. Its `conf` copies each installed template once and leaves an edited .conf alone, which is checked by running both scripts rather than by reading them. It stays open for the check that needs a clean Ubuntu and a clean Windows machine to reach 'ready' on all three apps, which nobody has run yet. The first retail client session, on 2026-09-17, passed the real-client checks for the session handshake, authentication with a wrong password and a retry, the empty character list and the shutdown notice. 3.25, added on 2026-09-17 at the maintainer's direction, is the Ambrose launcher that is now the only way the client is ever started: it passes its automated checks, and the first runs against the maintainer's own client, on 2026-09-17, showed the client ignoring a configuration that had been parsed and written again, so the launcher now changes only the values it needs in the template's own bytes and the client honours them; its windowed launch is recorded in part, with the login screen itself and its `--wait` check still waiting for a run with a login server. 3.24 is done: apps/clientdriver drives the maintainer's own client through a scenario with nobody at the keyboard, and the findings of its review are fixed with regression tests. On 2026-09-18 its four scenarios each ran clean on r806919, login-to-charselect twice from a clean state, every check passing and nothing left running, and an ordinary ctest run never starts a client, because each scenario test skips with 77 until AMBROSE_CLIENT_DIR names an install, the same opt-in the client-labelled C++ tests take. 17.07's log viewer and command console were run against a real panel on 2026-09-24: filtering, the ten second cut and the recall list pass, while pausing does not count what arrives and the console sends shutdown with no confirmation, so it stayed open on those two. The pause count and the confirmation landed the same day and both are verified against matching builds, so 17.07 is done: a command that cannot be undone is refused until an operator types yes, and anything else leaves it alone without sending a request. 17.01 is built: every console line carries its meaning in the sixteen terminal colors, the apps read key presses at an `Ambrose> ` prompt that is held to one row and scrolls a long command sideways rather than wrapping, and a log line arriving mid-command leaves the typed text alone. 17.02, 17.03 and 17.04 are done, and 17.06 is done: an app's admin API serves the panel, which signs in once by trading the admin token for a browser session, shows the app live with the age of every figure, and names the milestone of every page still to come. 17.08 is done: a supervisor runs the apps, takes back the ones still running when it starts again, and the panel starts, stops, restarts and kills them with a countdown, shows the output the supervisor captured for this run and the one before, lists every app's settings with the value in use, the shipped default and where each was read, and lists each database's applied and pending update files, applying the data-only ones live. Then, at the maintainer's direction on 2026-09-18, the panel's foundation is built before the rest of the game, so every later subsystem is built into it instead of being fitted to it afterwards: 17.01 the console, 17.74 the console's own log line, 17.02 the admin API, 17.03 status, 17.04 the live log stream and 17.06 the dashboard with its overview, which also gives 3.26 the launcher window its components. Then, because the maintainer asked on 2026-09-22 for panel accounts that can each be given any role, the accounts come first: 17.14, the panel's own listener and sessions on the supervisor 17.08 built, and 17.46, panel users and their sign-in, then 17.106, error reports that name the source file and line of each error and download as a file an operator sends to the maintainer, which the maintainer asked for the same day. 17.14 and 17.46 are both done: the supervisor serves the panel on a listener of its own with TLS, sessions, an audit store and a cost-weighted limit, and an operator signs in with their own name and Argon2id password, made once from a one-time link the supervisor prints on a first start with nobody in it. 4.01 is done: the game layer has its core, the world tick that owns the world thread and drains each session's queue on it, ScriptMgr with the hooks every later domain hangs off, and a script or a module that joins the build by being there rather than by an edit to a core file. 4.02 is built and passes every automated check: CommandMgr holds one tree of commands gathered from the CommandScripts, a line is split into the deepest command that matches and the arguments left over, a caller below a command's level is told there is no such command in the same words as one that does not exist, a command_security row overrides the level a script declared, and the console reaches the commands offered to it, with `server info` run against a real game server. Its one real-client check waits on 4.05 rather than on the maintainer, because nothing carries a typed line from the client to CommandMgr until the game server has its message handlers. That is enough to unblock 17.05, and after it 17.47-17.50: two-factor sign-in, roles, grants and their pages. 17.05 is done as of 2026-09-23: every app answers POST /api/command, running the line through the console table it already fills, which on a game server is every command CommandMgr holds, and writing down who ran what, from where, at which level and why it was refused, with a sensitive argument reaching neither the record nor the answer. Its console page is live with it, so a command typed in the panel is the same command the terminal runs. The audit table half waits for db_login to have one; the file is written now. Roles are what the maintainer asked for, so the detour through 4.01, 4.02 and 17.05 is what reaches them; when a panel milestone is blocked the blocking milestones are built and the panel is returned to, which is the maintainer's direction of 2026-09-22. 17.09 is done on 2026-09-24: src/common/Metric holds the counters, gauges and histograms a subsystem takes a reference to once at startup and updates with a single relaxed atomic afterwards, about eight nanoseconds against the fifty the milestone allows, and labels are what let one name cover every pool, service and reload target and still be summed or split apart by a reader. Every app answers GET /metrics in the Prometheus text exposition format behind the same guard as the rest of the admin API, which promtool 3.14.0 accepts on a running server's output, and the same register answers GET /api/metrics as JSON so the panel's Metrics page reads it the same typed way every other page reads its data. What is counted is the world tick, each service's messages and how long their handlers took, each pool's statements and their time, and each reload target's runs, failures and duration, with a target's counters registered when the target registers itself rather than when it first reloads, so a graph starts at zero instead of at whatever the first reload left behind. The live values the status register already published are written into the same scrape as gauges, so an operator graphs everything the process knows from one place rather than only what was counted with an atomic. Getting there needed the admin router to stop sending every path outside /api to the panel's files, because a scraper asks at /metrics and nowhere else: the file handler is now the fallback for paths no route claims, and a route registered outside /api is served and still guarded. 17.10 is done on 2026-09-25 from an outside contributor: apps/grafana brings up Prometheus and Grafana with the data source and three dashboards, realm overview, performance and database health, provisioned rather than clicked together, and doc/OPERATIONS.md is the runbook for it and for the admin API. Run against a gameserver from main, the scrape was up 13 seconds after `docker compose up` and every one of the eleven panels was drawing live data at 28. It needed one line the first version did not say: Prometheus asks for the host by name, and the admin API refuses a name it has not been given, so the runbook now says to add it. 17.19 is done on 2026-09-24 and is what those metrics were for: the supervisor samples every app it runs a few seconds apart, through a ProcessInfo that was taught to read any process rather than only the one asking, and writes processor share, memory, threads, open handles and disk bytes into a history of its own. Beside them it asks each app what the app counts about itself, so sessions, players, realms and how long a world tick took are graphed from the same place: a gauge is written as it stands, and a counter or a histogram is written as the difference from the last reading, which is a rate or the mean of that stretch rather than of every second since the server came up. The history is kept at two resolutions and a sample is folded into both as it arrives rather than by a sweep, so a supervisor that was stopped when a sweep was due loses nothing, and the coarse one is saved to disk and read back, replacing what a bucket holds rather than adding to it, so the same file read twice leaves a day in the long series exactly once. A stretch nothing was recorded in is absent and never zero, because an app that was stopped drew no samples and a zero would read as an app running and idle; the panel's Resources page says so in words as well as leaving the gap in the line. Reading a month costs 2.6 ms and comes back as the few hundred points a screen can draw, sampling twenty apps costs 420 microseconds a round, and the series the graph draws for a process holding half a core reads within three points of what that process measured on its own clock. 17.73 is done the same day, its last check earned rather than eyeballed: the three accessibility criteria an automated gate cannot judge now each have a browser test, and each was proved by breaking the thing and watching the right one fail. Two of the three had been passing for the wrong reason. The focused-row test used a sticky bar that never stuck, because its offset came from a utility class that is not generated for that file, so it asserted nothing; it now sets the offset itself, checks the bar is stuck before measuring against it, and focuses a row from just above the fold so the container's scroll padding is what decides. The paste test asserted a value it had assigned itself; it now asks only whether anything cancelled the paste. Carried onto the real sign-in page, that test found the admin token field telling password managers to stay away, which the settled rule forbids, and it now names its autofill purpose. 17.11 is done the same day: `--tui` draws full-screen panels in the terminal, status and published figures and problems across the top, the recent log under them and a command line at the foot, built with FTXUI. The layout is a function of the panels and the size of the window, so it can be rendered and read at any size without a terminal existing, and the loop takes where its panels, its size, its keys and its screen come from, which is how a test changes the size between two key presses and reads both paintings rather than trusting that a resize would work. The real terminal composes the same element and judges keys through the same function, so what the tests hold is what runs. Asking for it where the output is a file or a pipe is declined with a line saying the ordinary console is used instead, which was read off a real server rather than asserted. Then 3.26, the launcher's own window in the look doc/DESIGN.md sets, and after it 3.15 and 3.16, each gameplay milestone carrying its own panel surface under the rule above. 3.26's window is built on 2026-09-24: the launcher opens the operating system's own web view with `--window-ui`, WebView2 on Windows and WebKitGTK wherever the build finds one, and a machine with neither says so in one line and runs as the console launcher it always was. The page is read from a folder handed to the view rather than served, so no port is opened and the only traffic a run makes is the client's own to the login server, and the fonts ship with it. Every screen asks the same Prepare the console options reach, so the window and the terminal cannot disagree about what would be run, and a password is replaced with asterisks before the window is told anything, because the command is drawn on a screen that can be photographed. Where the window was left is remembered in a file of its own, and a place no screen holds any more is dropped rather than put back. It stays open for its real-client and network-capture checks, and for the account fields its deliverables name, which wait on the milestone that answers MSG_USER_VALIDATE: a box that looks as though it signs somebody in and does not is worse than no box. 3.15 and 3.16 are built the same day and are what phase 3 is named for. The login server reads the WizardCharacterCreationInfo a client sends, judges it against the schools, the name tables and the starting state loaded from the world database, which it now opens beside the login and characters ones, and writes the wizard and its appearance in one transaction with an id no wizard has ever held, answering MSG_CREATECHARACTERRESPONSE with 0 or 1 because those are the only two values the client is known to read; the reason is written to the log, where an operator can read it. Every appearance value is judged against the width and the options the type dump gives its property rather than against a number written in the server, so a revision that widens a field is followed by reloading the dump. How many wizards an account may hold and whether it may name one itself are read for every request, so lowering either takes hold at the next one with nothing restarted. Fifteen tests hold it, five of them driving a real session and a real database over loopback, where a refused request leaves the account exactly as it was and the session able to go on. The first run from the maintainer's own client on 2026-09-24 was refused, and what it refused turned out to be the server's own assumption: m_nameIndices is packed locale<<24 | first<<16 | middle<<8 | last, and the top byte the server had treated as unused carries 3 for en-US, which is the third locale the client's own CharacterNames.xml lists. It was measured rather than guessed, by choosing Finnigan Dragonwall, whose parts are positions 60, 14 and 240, and watching 0x033C0EF0 arrive; whether that ordering is the client's locale numbering rests on that one measurement and a run in another locale would settle it. What made it cheap was the refusal naming the value it saw rather than only that something was wrong. With that fixed, the whole chain ran from the maintainer's own client the same evening: sign in, create a wizard, see it on the select screen as a level 1 Novice Pyromancer, pick it, and be handed to the gameserver, which accepted the key and attached the session. 3.15 and 3.16 keep one real-client check each, the slot-limit refusal seen from the client, and the client then waited at a loading screen, because LOGINCOMPLETE was 4.14's and the six milestones from 4.09 to 4.14 that build a world to stand in had not started. That run also found the login server never overriding GetUpdateInterval, so its update tick never ran and it read realmlist exactly once at startup: ninety seconds after any restart every character pick was refused with no realm online. The tick is there now, and a refusal says how many realms the list holds, how many are online and how long ago it was read. 4.09 is done on 2026-09-25 and is the first of those six: sZoneMgr holds every zone template, the named places inside each zone and the objects placed in them, each a reload target rebuilt beside the store that is serving and swapped in whole, so `.reload zone_location` with a bad row names the row and changes nothing. Loaded from the maintainer's own install, which zone_extractor read with 1339 of 1339 zone files decoded, a gameserver starts with 1339 zones, 3892 named places and 25823 placed objects in under two seconds, and `.zone info WizardCity/WC_Hub` answers from them. A place a zone does not hold falls back to its Start rather than to nothing, because a wizard sent through a door that no longer exists must still stand somewhere; a location's direction is the one float the client's LocationTemplate gives, so it is read as a yaw, while an object's orientation is left unconverted where its shape is a quaternion, because the convention for turning that into a yaw has not been established. 4.10 is done the same day: a running instance of a zone is a Map with a dynamic zone id no other running instance holds and the wizards standing in it, sMapMgr finds a zone's public instance by its path or makes one, and an instance that empties is taken down by the world tick once Zone.UnloadDelay has passed, the delay being the one in force when it emptied. Each instance hands out its own sixteen-bit mobile ids, a low range for placed objects and an upper range for players, and a released id waits Zone.MobileIdReleaseDelay and then goes to the back of the line, because a fast leave-and-rejoin handed the id that just left races the new object's arrival against the old one's removal. Runtime GIDs start at 1<<52, above every id this server stores, and a placed object's permID is worked out from its zone, template and the id the zone's own data gives it, so it survives a restart and a fresh extraction alike. Object containers inside an instance arrive with 5.02's spawning, which is what fills them. On 2026-09-25 a wizard stood in Ravenwood for the first time: the client driver's enter-world scenario signs in with the r806919 client, presses Play, and the game server spends the key, loads the wizard, puts it in the zone's public instance and hands it its own object in MSG_LOGINCOMPLETE, in the CoreObject form 4.11 writes, until the client has loaded the zone and says so with MSG_CLIENTZONED. 4.11 keeps the checks that need a capture of a later schema. 4.12 is done the same day: the client's requests as it enters, for its timed access passes, its subscriber-only items and its crown balance, are answered with what the client's own handlers read, found in its code with Ghidra, and its notes about its screen, patch time, shopping and quest finder are taken, while every other world message a client sends is reported once per session rather than on every packet, which let the game server's drop budget return to the shared default. The same run is the real client session 4.04 to 4.07 waited on: it shows the client echo the key, account, wizard, zone and location it was sent to, close its login connection itself and attach, and the key accepted before LOGINCOMPLETE, which finishes 4.04, 4.05 and 4.07 and leaves 4.06 only its bad-key check. 4.13 is done the same day: a client that has not attached within Attach.Timeout of connecting is closed by the world tick, a key nobody issued and the right key naming another account's wizard are both refused with MSG_ATTACHFAILED and nothing after it, and a new driver scenario, enter-the-commons, enters WizardCity/WC_Hub at its Start and walks the wizard forward with the client's own W key, which leaves under a quarter of the view unchanged where standing idle leaves nearly all of it. The same run records the fields MSG_LOGINCOMPLETE carries, which is also where 4.14's own shared checks were earned, and 4.14 is done with it: the static zone objects it also carried belong to 5.02, which has the same checks and waits on 4.14 and on 5.01's template store, so 4.14 keeps only WLD-8's MSG_CLIENTZONED, as its title says. Phase 17's checks that wait for the maintainer are the ones it marks Dev-gated: a second machine or virtual machine for 17.22 and 17.42, a reboot for 17.23 and a Pterodactyl install for its egg, a clean desktop machine for 17.24, a desktop SFTP client for 17.40, a security key for 17.45, a Windows console and a Linux terminal for 17.01's colored-line check and 17.74's column check, and a retail client session for 17.21, 17.62 and 17.64. Each is run by hand and recorded with its milestone, and every other check in that phase runs automatically.

I checked these against the sources, read-only:
(1) Root.wad holds 29 *Messages*.xml files, not 26. GameMessages2.xml (svc 55, 10 msgs), WizardMessages2.xml (53, 254) and WizardMessages3.xml (56, 213) exist. That gives 1448 records, 1446 distinct ids and still only 9 field types. MSG_CLIENTZONED arrives as svc=53 order=64 right after LOGINCOMPLETE in a local session capture line 120, so the registry must load all 29 files.
(2) Ids come from an ordinal sort of element tags. The sort reproduces WIZARD MSG_UPDATEMANA=233 and MSG_UPDATEGOLD=231 (the duplicate PETHATCHREADYSTATUS gives 254 tags and 253 ids), GAME ATTACH=7, CLIENTMOVE=36, LOGINCOMPLETE=108, NEWOBJECT=122, BADGES=10, and QUEST ACCEPTQUEST=1. For WizCombat, CMB-1's claim 'MSG_SETSTATUS=last' is wrong: the last id is MSG_UPDATEDUELTIMER.
(3) The capture's login sequence (lines 1-12, VALIDATE at 21852) matches the LOG plan. But its CHARACTERSELECTED IP is 127.0.0.2:12333, which means the traffic was recorded against the reference server, not retail. What it proves is that the client accepted that behavior, not that retail behaves that way.
(4) KiStringHash('Fire')=2343174, 'Ice'=72777, 'Balance'=1027491821, 'class Duel'=85019234. LOG-7's StringId is the same function as OBJ-2, so they merge.
(5) The KI CRC variant gives 0x2DFD2D88 on '123456789'; zlib's standard CRC gives 0xCBF43926. One CRC module must support both.
(6) The Spells/* BINd entries I sampled use flags 7 (raw). TemplateManifest.xml uses flags 15 (zlib at offset 13), so the decoder needs both.
(7) In the r806919 dump, ResTeleport has 0 properties (so door destinations need an authored table). WizardQuestingBehaviorTemplate, ResDropTable, ResAddGold, WizZoneTriggers, Trigger and MobDeckBehavior(Template) are absent, so server-side schemas or types are needed. Duel has 62 properties, QuestTemplate 36, WizardCharacterCreationInfo 14 (hash 292458316).

Gaps the domain plans left open, filled here:
- There was no DAT plan, but DAT-1 and DAT-2 were referenced. They are now the KIWAD reader (1.13) and the template store (5.01).
- No owner existed for ScriptMgr, sWorld or CommandMgr (the 'CMD-1' reference). They are now 4.01 and 4.02.
- The shared GameEffect framework broke a WIZ-13 <-> CMB cycle. It is now 9.03.
- Fuzzy ids like 'WLD-interact', 'SOC-party' and 'EXT-1 (in QST-1)' are mapped to real milestones.
- Duplicates merged: ByteBuffer (FND-6+NET-1), BitStream (FND-6+OBJ-1), CRC32 (FND-7+PAT-2), Twofish (FND-8+LOG-3; Rec1 needs a 32-byte key, so Twofish-256), zlib (FND-8+OBJ-3), .lang loader (OBJ-14+QST-1), CoreObject envelope (OBJ-9+WLD-6), LocationString (LOG-11+WLD-1), zone object extractor (WLD-2+QST-4), object template extractor (OBJ-17+QST-3), SpellMgr (CMB-2+WIZ-9), loot (QST-19+CMB-20), vendors (EXT-1+QST-20+WIZ-24), pets in combat (CMB-26+EXT-22), tutorial (CMB-28+EXT-24), dispatch (NET-9+LOG-1).
- Ids are renumbered as <phase>.<nn>. Milestone titles keep their origin ids.

## The biggest thing first

Phase 1's protocol spine: a clean-room message layer loaded at runtime from the user's own 29 client XML files (KIWAD reader 1.13, then definition parser 1.14, then the runtime message registry 1.15), sitting on the frame and session codec (1.17-1.21). The decision on how CI builds without client files is settled: definitions load at runtime, so CI needs no client (see doc/ARCHITECTURE.md).

All 1446 message ids, every dispatch table and every client-visible milestone in all ten domains depend on correct (service, order) ids and field layouts. Those can only come from the client install, because no generated code may be committed. The build/CI model (client-gated build, a stub mode, or runtime-loaded definitions) changes the shape of shared/Messages, NET-9 tables, the tests and CI. Settling it late would mean rewriting every handler. It also carries the least-verified wire facts (SessionOffer 23 vs 28 bytes, long-frame semantics) that gate the very first real-client contact.

First milestones:

- 1.01 Toolchain hello: CMake + fmt + genrev (FND-1)
- 1.13 KIWAD archive reader (new DAT-1, absorbs PAT-2 KiwadHeader)
- 1.14 Message definition model and ordinal rules over all 29 XML files (NET-2)

## Phases

| Phase | Name | Milestones | Done when |
|---|---|---|---|
| 1 | [Foundations to first handshake](roadmap/phase-01-foundations-to-first-handshake.md) | 22 | The retail client, started with -L 127.0.0.1 12000 -P 0, completes SessionOffer/SessionAccept with Ambrose loginserver and stays connected. The server log names its MSG_USER_AUTHEN_V3 (7:27). |
| 2 | [Log in to character select](roadmap/phase-02-log-in-to-character-select.md) | 15 | The correct password lands on an empty character select screen. A wrong password shows the client's invalid-login dialog and allows a retry. |
| 3 | [Create, list and delete a wizard](roadmap/phase-03-create-list-and-delete-a-wizard.md) | 28 | A player creates a wizard (school, look, name). It appears on character select with the right appearance, persists across restarts, and can be deleted. |
| 4 | [A wizard stands in Ravenwood](roadmap/phase-04-a-wizard-stands-in-ravenwood.md) | 16 | After Play, the client leaves loginserver, attaches to gameserver and the player controls their wizard in WizardCity/WC_Ravenwood (or WC_Hub 'Start'). No other objects are streamed yet. |
| 5 | [The zone comes alive for one player](roadmap/phase-05-the-zone-comes-alive-for-one-player.md) | 8 | Ravenwood shows its NPCs and props where retail has them, and the HUD shows real health, mana, gold and level from the DB. Relogging returns to the same spot, and quit-to-select works without a password. |
| 6 | [Other players are visible](roadmap/phase-06-other-players-are-visible.md) | 18 | Two clients in WC_Hub see each other walk, jump, chat and emote, and one sees the other vanish on logout. Walking through the Ravenwood gate transfers zones. GMs teleport, kick and ban. |
| 7 | [An NPC offers a quest](roadmap/phase-07-an-npc-offers-a-quest.md) | 13 | A test NPC in Ravenwood shows '!' and plays its Prep dialog. It opens the quest offer window, and accepting fills the quest book. Talking to the persona NPC ('?') completes the goal and chains the next offer. |
| 8 | [The wizard grows: vitals, XP, spells, backpack and gear](roadmap/phase-08-the-wizard-grows-vitals-xp-spells-backpack-and-gear.md) | 14 | GM commands grant gold, XP, spells and items that show live. Potions work. Equipping a hat changes the model for nearby players. Decks and treasure cards can be built, and trainers teach spells. |
| 9 | [A duel completes](roadmap/phase-09-a-duel-completes.md) | 12 | A wizard walks into a wandering mob and is pulled onto a sigil. They draw cards, cast Fire Cat, pay pips, and either win (victory sequence, respawn) or lose (sent to safety), and can flee. |
| 10 | [Play the Wizard City opening quests](roadmap/phase-10-play-the-wizard-city-opening-quests.md) | 18 | A fresh wizard plays the first Wizard City chain from committed key-only SQL, covering offer, waypoint, persona, usage and bounty goals, rewards and gates opening. They also buy from vendors and use the Spiral Door. |
| 11 | [Real combat](roadmap/phase-11-real-combat.md) | 25 | Two players and several mobs fight with gear stats, blades, traps, shields, heals, DoTs, AoE, stuns and authored creature decks. Health bars never desync. |
| 12 | [Social wizards, instances and realms](roadmap/phase-12-social-wizards-instances-and-realms.md) | 23 | Players friend, whisper, group, trade, bank and dye. They wear titles, save outfits, pick instances and switch realms from the in-game picker. |
| 13 | [Pets, cantrips, crafting and minigames](roadmap/phase-13-pets-cantrips-crafting-and-minigames.md) | 21 | An equipped pet follows the wizard, eats snacks, levels up and casts in duels. Cantrips cast. Reagents are harvested and crafted. Kiosk minigames pay rewards, and the Crown Shop browses and sells. |
| 14 | [Dungeons, tutorial and PvP](roadmap/phase-14-dungeons-tutorial-and-pvp.md) | 16 | Groups enter sigil dungeons with countdowns. New wizards play the scripted tutorial, and players queue for ranked PvP, tournaments, pet derby and daily assignments. |
| 15 | [Housing, gardening and fishing](roadmap/phase-15-housing-gardening-and-fishing.md) | 21 | Players go home, decorate, store items in attic and vaults, grow gardens, fish ponds, publish castle tours and build castle magic. |
| 16 | [Patch server and tooling ownership](roadmap/phase-16-patch-server-and-tooling-ownership.md) | 13 | The retail client patches against Ambrose with 0 files altered, restores a deleted WAD, and streams missing zone packages. Users' type dumps come from Ambrose tooling (built in 3.21). This track can run in parallel any time after phase 2. |
| 17 | [Operations: console, admin API, dashboard and metrics](roadmap/phase-17-operations-console-admin-api-dashboard-and-metrics.md) | 111 | From a browser on a desktop or a phone, an operator sees every server's health and player counts, follows live logs, runs audited commands, edits game settings and reloads content live, restarts a crashed server, and reviews performance history. Like a game server hosting panel, it signs each operator in on the panel's own listener with their own account, two-factor sign-in and permissions, records every action in an activity log, runs scheduled restarts and backups, restores a backup, updates with one click and rolls back, edits files, and manages servers on several machines from one place. Built for Wizard101, it also manages realms and zones, players online, accounts, registration and password recovery, bans, characters, reports and mutes, world database edits, announcements and timed events, patch revisions and an installation-wide maintenance mode that still lets game masters in, and a desktop app starts everything from one icon. Runs in parallel: 17.01 after 1.20, 17.12 and 17.13 after 4.16, and the rest once their dependencies land, which for 17.14 and the panel milestones above it means after 3.23. Ids are allocation order; the dependency graph in the phase file gives the build order. doc/PANEL.md describes the panel these milestones build. |

Total: 387 milestones.

## Decisions needed

These block specific milestones. The maintainer decides each one, then this list and doc/ARCHITECTURE.md are updated.

None are open. A new one is listed here with the milestones it blocks until the maintainer settles it.


### Resolved

Settled on 2026-09-25 at the maintainer's direction to settle every open decision, and recorded under Decisions in doc/ARCHITECTURE.md: Time zones; Panel operations, covering the panel's scopes and roles, the console's level cap, the keyring, sealing and keyed hashes, outbound HTTP, certificates, the event socket, backups, S3 storage, login-screen countdowns, schedules on nodes, the file editor and archives, SFTP and remote pull, security keys, QR codes, trash and versions, database credentials, maintenance bypass, world edit exports, player registration, the patch signing key, the sequential ramp, search and the installation file; World threads, zone data and extracted tables; Content, SQL and releases, covering columns, pending SQL names, tools linking the database, crowns, the tutorial and revision checks; and Combat messages, covering MoveType and results the client resolves itself. Fifteen entries this list still carried had been settled earlier, in the paragraphs below, and were removed the same day.

Settled on 2026-09-17 at the maintainer's direction, and recorded under Decisions, Experimental features in doc/ARCHITECTURE.md: no client revision or type dump is pinned. Ambrose follows the revision of the user's install (3.23) and builds each revision's type dump from the user's own client program (3.21). GPL and AGPL tools and libraries may be used, starting with Unicorn in typeextract.


Settled on 2026-09-16 at the maintainer's direction that the project rejects nothing, and recorded under Decisions, Experimental features in doc/ARCHITECTURE.md: authored SQL may commit client identifiers but not display text, so a door destination table holding only identifiers may be committed; Ambrose builds its own type dumper, an opt-in tool that reads the user's own running client with the terms risk stated; an embedded Lua runtime runs the client-shipped minigame scripts from the user's own install; and battlegrounds, castle magic and monster magic stay in scope. The same day plain HTTP for the admin API beyond localhost became an opt-in setting, off by default, recorded under Decisions, Operations.


Settled on 2026-09-16 at the maintainer's direction to make CI cheaper, and recorded under Decisions, Continuous integration in doc/ARCHITECTURE.md: checks run daily, on pushes that change CI files and on pull requests; builds run on a schedule when code changed, by `ci:` label or on demand; and a repository variable stops builds.

Settled on 2026-09-16 under the maintainer's standing direction, and recorded under Decisions, Message dispatch and session states in doc/ARCHITECTURE.md: sessions share the statuses Connected, Authenticated, CharacterSelected, LoggedIn and InWorld, tables name messages by tag and are checked against the loaded definitions at startup, and only protocol violations count strikes.

Settled on 2026-09-14 under the maintainer's standing direction, and recorded under Decisions, Accounts and the console in doc/ARCHITECTURE.md: the password verifier is stored and encrypted at rest when verifier keys are configured, AccountMgr builds as the `accounts` library in `game/Accounts` that the login server links, and security levels use AzerothCore's 0-4 numbering.

Settled on 2026-09-14 at the maintainer's direction, and recorded under Decisions, Live reload and live settings in doc/ARCHITECTURE.md: everything that can change while a server runs changes live, gameplay values are typed live settings edited from the control center, live settings persist in the database the app owns with an audit table, environment and command-line layers lock a key, and live world database edits are journaled and exportable as pending SQL. New milestones 4.15 (reload framework), 4.16 (live settings registry), 17.12 (settings and reload admin API), and 17.13 (dashboard settings editor) carry the work.

Settled on 2026-09-13 with the maintainer's direction to favor the most capable option, and recorded under Decisions, Operations in doc/ARCHITECTURE.md: Crow for the admin API, TypeScript and Svelte with Vite for the dashboard, an Ambrose supervisor for process control, and localhost-only access unless TLS and a token are configured.


Settled on 2026-09-13 and recorded under Decisions in doc/ARCHITECTURE.md: runtime-loaded protocol definitions and type dump with startup-validated declarations, the stack and libraries, the database server and connector, SQL update execution and hashing, tool languages, config documentation, and tools linking the database layer.


## Top risks

- Unverified wire basics could block the first real-client contact: SessionOffer length (23 vs 28 bytes), long-frame length semantics, and the Rec1/CK1 salt number formatting. 1.18 and 2.14 must be checked with a real client before anything else builds on them.
- The only capture comes from the reference server (CHARACTERSELECTED to 127.0.0.2:12333), not retail. It proves what the client accepts, not what retail sends, so ordering, error codes and blob masks inferred from it may be wrong.
- Message coverage: the domain plans counted only 26 of the 29 XML files. WIZARD2 (53), WIZARD3 (56) and GAME2 (55) hold 477 more records, including MSG_CLIENTZONED (53:64), which is needed at world entry. A duplicate tag shifts WIZARD ids after position 122, so each ordinal must stay test-guarded.
- Much content is absent from the client and must be authored clean-room, or imported at runtime from the user's own copy of another project's data set, which is never committed: quest templates, drop tables, vendor stock, creature decks, trainer lists, badges, door destinations (ResTeleport has 0 properties), and server-only classes (Trigger, WizZoneTriggers, Res*, WizardQuestingBehaviorTemplate, MobDeckBehavior are all missing from the dump). This content work is large. Since 2026-09-16 authored SQL may commit client identifiers but not display text.
- Combat math parity: if the client replays CombatActions with its own resolver, any formula mismatch shows as visible health desync. The formulas exist in no local source and need client RE.
- No way to test in hosted CI: the builds, type registry, extractors and real-client tests all need the user's install and dump, so regressions may only surface on the maintainer's machine.
- ObjectProperty edge cases: the per-field envelope policy, DirtyEncode semantics, Matrix3x3 width, and CoreObject block/type pairs other than 104/2 are unverified. Getting any of them wrong crashes the client rather than failing gracefully.
- Settings the client mirrors or simulates, such as combat constants, MSG_SETST values, and fields fixed at LOGINCOMPLETE, can desync if changed live. Each such setting must either push the change to connected clients or apply from the next duel or session, and its documentation must say which.
- Scope: about 300 milestones, with phases 13-15 (pets, housing, PvP) holding the most poorly documented systems. Every phase stays planned. Keep the phase gates honest and take later phases in order once phase 10's playable loop exists, apart from the parallel tracks of phases 16 and 17.

## Review findings not tied to one phase

**Missing work**

- Live settings and reloads: a typed settings registry, a reload framework, settings and reload endpoints, and a dashboard editor did not exist in the plan. **Planned:** 4.15, 4.16, 17.12, and 17.13, with reload paths and live settings added to the milestones that load stores or use tunable values.

- Unassigned LOGIN messages: MSG_CHANGECHARACTERNAME (order 26), MSG_SAVECHARACTER (23), MSG_FULFILLPROMOCODE (29), MSG_WEBCHARACTERINFO (21). Also character rename (WIZ3 MSG_REQUESTRENAMECHARACTER) and purchased character slots (WIZ2 MSG_UPDATEPURCHASEDCHARACTERSLOTS, ResAddCharacterSlotResult).
- Most of WizardMessages2 (53) and WizardMessages3 (56), 467 messages, has no milestone even though the roadmap itself flags them. Missing systems include jewels/sockets (EQUIPJEWELTOITEM, UNLOCKSOCKETS, UNSOCKETJEWELREQUEST), mounts (RIDEOBJECT, SETSTOREDMOUNT), Team Up/Team Help (REQUESTTEAMUP, TEAMUP*), resume-instance (SETRESUMEINSTANCE, UPDATERESUMEINSTANCETIME), zone gates/recall (ZONEGATELIST, ZONEHOP, RecallLocationConfirm, GotoDormConfirm), guilds/kinhouses (GUILD*, VISITGUILDHOUSE), Pixie Post mail, loyalty store, season pass, Rate My Stitch, Magic Mirror appearance change, spell fusion (ADDSPELLFUSIONTODECK), tiered spell upgrade/refund, Hatchmaking, pet elixirs, class projects/gauntlets, basic chat channels, zone tokens (AddZoneToken/ModifyZoneToken), environmental damage, holiday/event currency (HOLIDAYDATA, UPDATEEVENTCURRENCY1/2), and GameMessages2 (55) MSG_BADGESEGMENT and MSG_PVPBLOCK.
- Server-side use of zone geometry. Each zone WAD ships collision.bcd, zone.nav, pathData.xml, pathNodeData.bin, spawnData.xml, clientSpawnData.xml, portals.xml and FishingInfo.xml (verified in WizardCity-WC_Ravenwood.wad). No milestone loads collision or navmesh for mob wandering, aggro line of sight, spawn ground snapping, fishing spots or move validation.
- Crash safety and persistence policy: periodic autosave of all online characters, write-behind queue flush on shutdown or crash, and reconciling stale online=1 / login_key rows after a gameserver crash (otherwise the character stays locked). No DB backup or restore guidance. **Planned:** 17.61 reconciles the stale online, realm_online_character, login_key and account_session rows after a crash, scoped to the crashed realm and fenced by run generation; 17.16, 17.51 and 17.52 cover consistent dumps, a checked restore and retention, with the guidance in doc/OPERATIONS.md from 17.10. Periodic autosave is 5.03's save interval, and 17.08's graceful stop asks each app to save and drain before it exits.
- Player-facing account lifecycle and security: account registration path beyond the console, password change/reset, login brute-force throttling per IP/account, and audit logs of GM actions and chat for moderation (reports/Infraction, MSG_REPORTHOUSE handling). **Planned:** 17.62 adds player registration, email verification and player-driven password reset, 17.21 the operator-side account, ban and character pages, and 17.63 the report queue, chat search and mute history over 12.07's records. Login throttling per address and account is already built in 2.14, and 17.14 and 17.46 throttle panel sign-in. Whether the panel offers registration at all is listed under Decisions needed.
- Remote administration (AzerothCore RA/SOAP equivalent) or an operator console on each app, plus metrics/health endpoints for multi-realm operation. **Planned in phase 17.**
- Message identity collisions: service 54 (CatchAKeyMessages.xml) and service 44 (ShockALockMessages.xml) both define MSG_MG3_CONNECT/MOVED/REWARDS. GAME also has duplicate MSG_REMOVEOBJECT tags with different descriptions (S->C out of proximity vs C->S remove instance object). The registry and msggen must key by (service, name) and handle direction. Only the PETHATCHREADYSTATUS duplicate is mentioned. **Resolved in 1.14 and 1.15:** definitions and declarations key by (service, tag), duplicates merge only with identical fields, and the phase 2 dispatch tables curate direction.
- Localization and locale for server-generated text (MSG_ATTACH carries Locale). **Planned:** 17.66 localizes the dashboard's own strings, with a per-user locale and a test that fails when a string has no key in the default catalog. Server-side notice strings in each session's locale stay planned, not yet scheduled.
