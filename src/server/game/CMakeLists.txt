# Project Ambrose by Imjustchico
# Adds the game layer's libraries: globals, shared by every game system, and accounts and characters, each built on the database layer, which the login server links on their own.
add_subdirectory(Globals)
add_subdirectory(Accounts)
add_subdirectory(Characters)
