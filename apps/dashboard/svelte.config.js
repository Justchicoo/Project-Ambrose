/*
 * Project Ambrose by Imjustchico
 * The panel's Svelte settings, which are the repository's own, so it compiles exactly as the shared components do.
 */

export { default } from "../../svelte.config.js";
