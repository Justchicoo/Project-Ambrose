/*
 * Project Ambrose by Imjustchico
 * The launcher window's Svelte settings, which are the repository's own, so it compiles exactly as the panel does.
 */

export { default } from "../../svelte.config.js";
