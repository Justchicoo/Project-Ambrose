/*
 * Project Ambrose by Imjustchico
 * The package's Svelte settings, which are the repository's own, so the gallery and the browser tests compile in runes mode like the surfaces do.
 */

export { default } from "../../svelte.config.js";
